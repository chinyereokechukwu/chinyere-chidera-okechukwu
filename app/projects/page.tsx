import type { Metadata } from "next"; import { PageHero } from "@/components/page-hero";import { ProjectCard } from "@/components/cards"; import projects from "@/data/projects.json";
export const metadata:Metadata={title:"Projects"};export default function Projects(){return <><PageHero eyebrow="Research projects" title="Ideas designed for impact." summary="Current and emerging projects connect information governance with sustainable organisational transformation."/><section className="section"><div className="container grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(290px,1fr))"}}>{projects.map(p=><ProjectCard item={p} key={p.title}/>)}</div></section></>}
