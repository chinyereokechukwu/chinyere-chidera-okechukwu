import type { Metadata } from "next";import { Download } from "lucide-react";import { PageHero } from "@/components/page-hero";import downloads from "@/data/downloads.json";
export const metadata:Metadata={title:"Downloads"};export default function Downloads(){return <><PageHero eyebrow="Download centre" title="Portfolio materials, ready to share." summary="A focused set of professional materials for collaborators, employers, funders and conference organisers."/><section className="section"><div className="container"><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))"}}>{downloads.map(d=><a className="card" href={d.file} key={d.title} download><Download color="var(--gold)"/><div className="eyebrow" style={{marginTop:16}}>{d.type}</div><h2 className="serif" style={{fontSize:26,margin:"7px 0"}}>{d.title}</h2><p>{d.description}</p></a>)}</div><p style={{marginTop:28,fontSize:14}}>Files will be available here when uploaded to the public downloads folder.</p></div></section></>}
