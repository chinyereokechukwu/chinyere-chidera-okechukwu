import type { TimelineEntry } from "@/types";
export function Timeline({items}:{items:TimelineEntry[]}) { const orderedItems = [...items].sort((a, b) => b.sortYear - a.sortYear); return <ol style={{padding:0,margin:0,listStyle:"none",borderLeft:"1px solid var(--line)"}}>{orderedItems.map(item=><li key={`${item.year}-${item.title}`} style={{position:"relative",padding:"0 0 32px 28px"}}><span style={{position:"absolute",left:-6,top:4,width:11,height:11,borderRadius:"50%",background:"var(--gold)"}}/><div className="eyebrow">{item.year}</div><h3 className="serif" style={{fontSize:21,margin:"6px 0"}}>{item.title}</h3><strong style={{fontSize:14}}>{item.organisation}</strong><p style={{marginBottom:0,opacity:.8}}>{item.description}</p></li>)}</ol>; }
