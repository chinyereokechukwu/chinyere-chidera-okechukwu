import type { Metadata } from "next"; import { PageHero } from "@/components/page-hero"; import { PublicationList } from "@/components/publication-list"; import publications from "@/data/publications.json"; import type { Publication } from "@/types";
export const metadata:Metadata={title:"Publications"}; export default function Publications(){return <><PageHero eyebrow="Scholarly output" title="Publications, proceedings and manuscripts." summary="A curated record of interdisciplinary work across sustainability reporting, AI, digital ethics, systems thinking and knowledge management."/><section className="section"><div className="container"><PublicationList items={publications as Publication[]}/></div></section></>}
