import Link from "next/link";
import { ArrowUpRight, BookOpen, CalendarDays } from "lucide-react";
import type { Project, Publication } from "@/types";
export function PublicationCard({item}:{item:Publication}) { return <article className="card"><div style={{display:"flex",justifyContent:"space-between",gap:12,color:"var(--gold)",fontSize:12,fontWeight:700}}><span>{item.type}</span><span>{item.year}</span></div><h3 className="serif" style={{fontSize:22,margin:"12px 0 8px"}}>{item.title}</h3><p style={{fontSize:14,opacity:.78}}>{item.authors} · <i>{item.venue}</i></p><p style={{lineHeight:1.6}}>{item.abstract}</p><div style={{display:"flex",gap:8,flexWrap:"wrap"}}>{item.topics.map(t=><span key={t} style={{fontSize:12,background:"var(--mist)",padding:"5px 8px",borderRadius:99}}>{t}</span>)}</div></article>; }
export function ProjectCard({item}:{item:Project}) { return <article className="card"><div className="eyebrow">{item.label} · {item.period}</div><h3 className="serif" style={{fontSize:24,margin:"12px 0"}}>{item.title}</h3><p>{item.description}</p><div style={{display:"flex",gap:7,flexWrap:"wrap"}}>{item.themes.map(t=><span key={t} style={{fontSize:12}}>{t} <span style={{color:"var(--gold)"}}>•</span></span>)}</div></article>; }
export function ActionCard({title,description,href,icon="book"}:{title:string;description:string;href:string;icon?:"book"|"calendar"}) { const Icon=icon==="book"?BookOpen:CalendarDays;return <Link href={href as never} className="card" style={{display:"block"}}><Icon color="var(--gold)"/><h3 className="serif" style={{fontSize:22,margin:"14px 0 8px"}}>{title} <ArrowUpRight size={17}/></h3><p>{description}</p></Link>; }
